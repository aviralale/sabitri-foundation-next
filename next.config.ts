import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "randomuser.me",
      "sabitrifoundation.org",
      "ui-avatars.com",
      "bisaalchaurasia.com",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
